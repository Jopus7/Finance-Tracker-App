name: CI checks

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Docker Compose Build
        run: docker compose -f docker-comopse.yml build
